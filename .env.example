# Backend origin for the dev proxy. The browser only ever talks to the admin
# origin (/api/v1 is rewritten to this host), so httpOnly auth cookies work
# without CORS.
BACKEND_ORIGIN=http://localhost:3000
